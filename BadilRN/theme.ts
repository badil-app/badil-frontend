import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    pageBg: {
        backgroundColor: "#f2fcf5",
    },
    button: {
        color: "#dcf4e4",
    },
    modal: {
        backgroundColor: "#b4eec5",
    },
    modalButton: {
        color: "#fcf2f9",
    },
    // Add more styles as needed
});

export default globalStyles;
